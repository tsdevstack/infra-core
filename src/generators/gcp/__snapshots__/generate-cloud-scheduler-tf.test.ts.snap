// Rstest Snapshot v1

exports[`generateCloudSchedulerTf > snapshot > should match snapshot for empty jobs 1`] = `
"# No scheduled jobs configured
# Add scheduledJobs in infrastructure.json to generate Cloud Scheduler resources
"
`;

exports[`generateCloudSchedulerTf > snapshot > should match snapshot for single job 1`] = `
"# Cloud Scheduler Infrastructure
# Generated by: npx tsdevstack infra:generate
#
# Scheduled jobs call service endpoints with OIDC authentication.
# Services validate tokens using SchedulerGuard from @tsdevstack/nest-common.

# ─────────────────────────────────────────
# Data Sources (reference existing Cloud Run services)
# ─────────────────────────────────────────

# Data source for existing auth-service Cloud Run service
data "google_cloud_run_v2_service" "auth_service" {
  name     = "auth-service"
  location = var.gcp_region
}

# ─────────────────────────────────────────
# Service Account
# ─────────────────────────────────────────

# Service account for Cloud Scheduler
resource "google_service_account" "scheduler" {
  account_id   = "\${var.project_name}-scheduler"
  display_name = "Cloud Scheduler Service Account"
  description  = "Service account for Cloud Scheduler to invoke Cloud Run services"
}

# ─────────────────────────────────────────
# IAM Bindings
# ─────────────────────────────────────────

# Allow scheduler to invoke auth-service
resource "google_cloud_run_v2_service_iam_member" "scheduler_auth_service" {
  name     = data.google_cloud_run_v2_service.auth_service.name
  location = var.gcp_region
  role     = "roles/run.invoker"
  member   = "serviceAccount:\${google_service_account.scheduler.email}"
}

# ─────────────────────────────────────────
# Scheduled Jobs
# ─────────────────────────────────────────

# Scheduled job: cleanup-tokens
resource "google_cloud_scheduler_job" "cleanup_tokens" {
  name     = "cleanup-tokens"
  schedule = "0 */4 * * *"
  time_zone = "UTC"

  http_target {
    uri         = "\${data.google_cloud_run_v2_service.auth_service.uri}/jobs/cleanup-tokens"
    http_method = "POST"

    oidc_token {
      service_account_email = google_service_account.scheduler.email
      audience              = data.google_cloud_run_v2_service.auth_service.uri
    }
  }

  retry_config {
    retry_count          = 3
    min_backoff_duration = "5s"
    max_backoff_duration = "3600s"
    max_doublings        = 5
  }

  attempt_deadline = "300s"

  depends_on = [google_cloud_run_v2_service_iam_member.scheduler_auth_service]
}
"
`;
